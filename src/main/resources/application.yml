spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/habitdb
    #url: jdbc:postgresql://host.docker.internal:5432/habitdb
    username: habit
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  redis:
    host: localhost
    port: 6379

server:
  port: 8082